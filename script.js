//console.log("this file is made in vscode and run by the use of terminal through node since both termiinal and vs code are in the same directory or folder type node filename in terminal to run this program");
  
//const someValue = require("./math");
//console.log(someValue);

//const math = require("./math");
//console.log(math.sum(2, 2));
//console.log(math.PI);


//we are now exporting between the directories
// const info = require("./Fruits");
// console.log(info);

import{sum,PI} from "./math.js";

console.log(sum(1,2));